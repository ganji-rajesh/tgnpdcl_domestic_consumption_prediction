# ============================================================================
# TGNPDCL Electricity Forecasting - Streamlit Deployment
# ============================================================================
# Optimized for Streamlit Cloud & Hugging Face Spaces
# Minimal dependencies for FAST cold starts (~15 seconds)
# ============================================================================

# Core Data Processing (REQUIRED for loading & manipulation)
numpy>=2.0.0,<3.0.0
pandas>=2.1.0,<3.0.0

# Time Series Model Support (for unpickling trained models)
statsmodels>=0.14.0,<1.0.0

# Metrics & Model Evaluation
scikit-learn>=1.3.0,<2.0.0

# Web Framework (REQUIRED)
streamlit>=1.39.0,<2.0.0

# Visualization (lightweight, no heavy dependencies)
plotly>=5.18.0,<6.0.0

# ============================================================================
# EXCLUDED (Heavy, not needed for inference):
# ============================================================================
# torch                   # Deep learning (not needed, using classical models)
# pytorch_lightning       # Training framework (inference only)
# darts                   # Time series library (uses torch)
# pmdarima                # Auto ARIMA tuning (model already trained)
# seaborn                 # Heavy viz lib (plotly is lighter)
# mlflow                  # Experiment tracking (deployment only, not needed)
# jupyter                 # Notebooks (Streamlit app, no jupyter needed)
